##################################################
# General definitions
##################################################

[general]

metadata = ~/.offlineimap
accounts = poczta
maxsyncaccounts = 3
ui = Basic
ignore-readonly = no
socktimeout = 60
maxconnections = 3

##################################################
# Mailbox name recorder
##################################################

[mbnames]

enabled = no
#filename = ~/.mutt/muttrc.mailboxes
#header = "mailboxes " 
#peritem = "=%(accountname)s/%(foldername)s" 
#sep = " " 
#footer = "\n" 

ui = Noninteractive.Basic

statuschar = .

##################################################
# Accounts
##################################################

[Account poczta]
localrepository = pocztaLocal
remoterepository = pocztaRemote

[Repository pocztaLocal]
type = Maildir
localfolders = ~/mail
sep = /
restoreatime = no

[Repository pocztaRemote]
type = IMAP
status_backend = sqlite
remotehost = poczta.email.pl
ssl = yes
remoteport = 993
remoteuser = ---@---.pl
remotepass = ---
maxconnections = 3
holdconnectionopen = no
nametrans = lambda foldername: re.sub('^Drafts', '_Robocze', re.sub('^Trash', '_Kosz', (re.sub('^Sent', '_Wysłane', re.sub('^INBOX', '_Odebrane', foldername)))))
